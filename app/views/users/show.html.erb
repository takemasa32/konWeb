<div class="container mt-5">
  <h1 class="mb-4">ユーザー情報</h1>

  <div class="mb-3">
    <p><strong>メールアドレス：</strong><%= current_user.email %></p>
  </div>
  <div class="text-center">
    <%= link_to "管理画面へ", blogs_path, class: "btn btn-secondary btn-lg mt-3" %>
  </div>
  <div class="mb-3">
    <%= link_to 'ログアウト', logout_path, method: :delete, class: 'btn btn-secondary' %>
  </div>

  <div class="mb-3">
    <%= link_to '登録解除', unsubscribe_path, method: :delete, data: { confirm: '本当に登録解除しますか？' }, class: 'btn btn-danger' %>
  </div>
</div>